apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: monitoring
spec:
  instances: 1
  bootstrap:
    initdb:
      database: grafana
      owner: grafana
      secret:
        name: grafana-db-secret
  storage:
    size: 1Gi
